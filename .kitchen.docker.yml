---
driver:
  name: docker

verifier:
  name: inspec

platforms:
- name: ubuntu
  run_list:
  - recipe[apt]

suites:
  - name: default
    run_list:
      - recipe[rvm::default]
    verifier:
      inspec_tests:
        - test/integration/default
    attributes:
